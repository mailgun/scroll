language: go
go:
  - 1.9
install:
  - docker-compose up -d
  - go get -u github.com/golang/dep/cmd/dep
  - dep ensure
script:
  - go test . ./vulcand/
  - docker-compose down
sudo: required
services:
  - docker
notifications:
  email: false
